export const config = {
  // the folder where the generated blocks will be stored
  outputFolder: 'blocks',

  // the folder where the templates are stored
  templatesFolder: {
    blocks: 'templates/blocks',
  }
};